---
import { Locale } from '../i18n/consts'
export interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(Locale, {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
